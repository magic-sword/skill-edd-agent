# プロジェクト開発ルール

## 1. スキル生成の原則
* 新規にスキル（検証用のダミースキルを含む）を作成・生成する際は、手動でコード（`main.py` 等）や仕様書（`SKILL.md`）を記述してはならない。
* 必ず `src/skills/skill-generator`（またはインプロセスツール `generate_skill`）を呼び出して自律生成すること。これにより、引数や状態の処理におけるADK 2.0評価システムとの不整合を未然に防止する。
