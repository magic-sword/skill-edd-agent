# ==============================================================================
# Gemini クライアント設定（.env.example）
#
# このファイルを「.env」という名前でプロジェクトルートにコピーし、必要な値を設定してください。
# ==============================================================================

# ------------------------------------------------------------------------------
# クライアントの接続先切り替え
# ------------------------------------------------------------------------------
# gemini: Google AI SDK を使用して Gemini API に直接アクセスします（要 API キー）
# agy   : ローカルの Antigravity CLI (agy) を経由して LLM を呼び出します（IDE のクレジット/認証を共有）
GEMINI_CLIENT_TYPE="agy"

# ------------------------------------------------------------------------------
# API キー設定 (GEMINI_CLIENT_TYPE="gemini" の場合のみ必須)
# ------------------------------------------------------------------------------
# Google AI Studio (https://aistudio.google.com/) から取得した API キーを設定します。
GEMINI_API_KEY="AIzaSyYourActualAPIKeyHere"

# ------------------------------------------------------------------------------
# LLM モデルとリトライ設定 (GEMINI_CLIENT_TYPE="gemini" の場合に適用)
# ------------------------------------------------------------------------------
GEMINI_DEFAULT_MODEL="gemini-2.5-flash"
GEMINI_MAX_RETRIES="3"
GEMINI_RETRY_DELAY="2.0"
