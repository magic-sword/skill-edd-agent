あなたはAIエージェントのスキル設計と実装の整合性を検証する専門家です。
以下の設計仕様と実装ファイルを確認し、整合性が取れているか評価してください。

## 設計仕様 (design.json)
```json
{design_json}
```

## 実装ファイル (scripts/models.py)
```python
{models_py}
```

## 実装ファイル (scripts/handler.py)
```python
{handler_py}
```

## 実装ファイル (scripts/executor.py)
```python
{executor_py}
```

上記の情報を基に、設計仕様と実装の以下の項目について整合性を検証し、その結果を厳格にJSON形式で出力してください。

- スキル名、説明が一致しているか
- 公開関数名、その説明、パラメータ（名前、型、説明、必須/任意、制約）が `design.json` と `handler.py` および `models.py` で一致しているか
- 応答パラメータ（名前、型、説明、制約）が `design.json` と `models.py` で一致しているか
- ADK 2.0 の「薄いハンドラーとエグゼキューターの分離」規約が守られているか
- `models.py` が `design.json` の応答スキーマを正確に表現しているか
- その他のADK 2.0 開発規約（関心の分離、オブジェクト指向など）に沿っているか

出力は以下のJSONスキーマに従ってください。

```json
{
  "status": "string", // 'success' または 'failed'
  "details": "string", // 検証が失敗した場合の不足事項やフィードバック詳細。成功時は空文字列。
  "score": "float" // 設計と実装の整合性スコア（0.0〜1.0）。
}
```