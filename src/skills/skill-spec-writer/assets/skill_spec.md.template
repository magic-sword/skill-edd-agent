---
name: ${skill_name}
description: ${mechanical_description}
---

# ${skill_name}

## 概要

${human_overview}

## トリガー条件

${trigger_conditions}

## AIエージェント向け使用方法

### 1. 実行手順（Instructions）

${execution_instructions}

### 2. 呼び出し方法
- **インプロセス呼び出し (Python API)**: ロード関数 `process_message` を使用（状態の `validated_input` に `Input` スキーマを設定して実行）。
- **サブプロセス呼び出し (CLI)**: CLIランナーを使用。
  `python3 -m edd_agent_tools.cli.run ${skill_name} [オプション引数]`
- **出力形式 (Output Mode)**: `${output_mode}` (${output_mode_description})

### 入力パラメータ

${input_parameters}

${constraints_section}
