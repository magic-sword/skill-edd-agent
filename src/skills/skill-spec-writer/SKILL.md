---
name: skill-spec-writer
description: 設計情報（Pydanticスキーマ等）を動的にロードし、ADK 2.0仕様に準拠したSKILL.mdを生成します。
---

# skill-spec-writer

## 概要

既存のスキル設計情報（design.json）を基に、ADK 2.0に準拠したSKILL.md仕様書を自動生成します。これにより、開発者はドキュメント作成の手間を省き、常に最新かつ正確なスキル仕様書を維持できます。

### 主な機能
* 既存のスキル設計情報（design.json）を動的にロードし解析します。
* Google ADK 2.0仕様に準拠したSKILL.mdドキュメントを自動生成します。
* スキルタイプ（ツール、エージェント、ワークフロー）に応じた適切な仕様書ライターを選択し、生成します。
* LLM（Gemini API）を活用して、目的、機能、トリガー条件、ワークフロー手順などの非決定論的なテキスト情報を抽出します。
* 入力パラメータ、出力パラメータ、制約事項などの決定論的な情報をMarkdown形式で整形し、仕様書に含めます。

### 内部処理の流れ
1. スキル名またはdesign.jsonのパスから対象スキルのディレクトリ構造を特定し、設計メタデータをロードします。
2. 出力ディレクトリとソースコードディレクトリのパスを解決し、必要に応じてデフォルト値を適用します。
3. スキルの実行タイプ（ツール、エージェント、ワークフロー）に基づいて、適切な仕様書ライター（SpecWriter）を生成します。
4. LLMに渡すプロンプトを構築し、必要に応じてソースコードを添付してGemini APIにリクエストを送信します。
5. Gemini APIからの応答をパースし、スキルの目的、機能、トリガー条件、内部処理の流れなどのテキスト情報を抽出します。
6. 抽出された情報と設計メタデータ（パラメータ、出力、制約など）を組み合わせて、最終的なSKILL.mdのMarkdownコンテンツをレンダリングします。
7. 生成されたMarkdownコンテンツを指定された出力ディレクトリにSKILL.mdとして保存します。

## トリガー条件

- このスキルの仕様書を生成して
- `{スキル名}`のADK 2.0仕様書を作成してください
- `design.json`ファイルからSKILL.mdを自動生成してほしい
- スキル`{スキル名}`のドキュメントを更新して
- 指定された設計パス`{design_path}`に基づいて仕様書を出力して
- スキル設計情報からSKILL.mdを作成するツールを実行して

## AIエージェント向け使用方法

### 1. 実行手順（Instructions）

あなた（エージェント）がこのスキルをトリガーした場合は、以下の手順に従ってください。

1. 必要な入力パラメータ（`prompt`, `design_path`など）を決定します。
2. 決定したパラメータを指定して、このスキルを起動してください。
3. 実際の処理（API呼び出しやファイル出力など）は内部スクリプト側で完結するため、あなた自身が内部テンプレートを読み込んで推論したり、成果物を手動で組み立てたりする必要はありません。

> [!NOTE]
> **`prompt` パラメータの使用ガイドライン:**
> * **指定可能な指示**: 生成される仕様書のテキスト説明のトーンや粒度（例: 開発者向けに詳細に、あるいは初心者向けに簡単に書く）、特別に言及してほしいセキュリティ設計や注意点の強調を指定できます。
> * **構造的な制約（指定不可）**: 仕様書のマークダウンレイアウト構成（見出しの順序、テーブル項目）は決定論的テンプレートに固定されているため、プロンプト指示によって構成そのものを変更することはできません。


### 2. 呼び出し方法
- **インプロセス呼び出し (Python API)**: ロード関数 `process_message(params, tool_context)` を使用。
- **サブプロセス呼び出し (CLI)**: CLIランナーを使用。
  `python3 -m edd_agent_tools.run skill-spec-writer [オプション引数]`
- **出力形式 (Output Mode)**: `STRUCTURED_JSON` (特定のJSONスキーマ構造に厳密に従った構造化データを出力します。生成結果のパース成功時に生成されたファイルのパスや、エラー時にはエラーメッセージと詳細情報が含まれます。)

### 入力パラメータ

| パラメータ名 | 型 | 必須 | 説明 |
|---|---|---|---|
| prompt | str | いいえ | 仕様書生成における、特別に明記したい追加の表現上のこだわりや注意点などの指示。 |
| design_path | str | いいえ | design.json ファイルの直接のパス。省略された場合は skill から自動探索します。 |
| skill | str | いいえ | 対象の既存スキル名。design_path 省略時の自動探索キーとして使用されます。 |
| output_dir | str | いいえ | 生成されたSKILL.mdを保存するディレクトリのパス。省略時は対象スキルのディレクトリに出力されます。 |
| source_code_dir | str | いいえ | 実装ソースコードが格納されたディレクトリパス（または単一ファイル）。指定しない場合、自動的にスキルの scripts ディレクトリを探索します。 |

### 出力パラメータ (構造化JSONの戻り値構造)

| パラメータ名 | 型 | 必須 | 説明 |
|---|---|---|---|
| status | Literal['success', 'failed'] | はい | スキルの実行結果ステータス。'success' または 'failed'。 |
| message | str | はい | 実行結果に関する詳細メッセージ。 |
| output_dir | str | はい | 仕様書(SKILL.md)が格納されたスキルディレクトリの絶対パス |


