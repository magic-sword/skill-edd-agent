# スキル仕様書（SKILL.md）デザインポリシー (Document Design Policy)

このドキュメントは、生成される成果物である **`SKILL.md`（仕様書ドキュメント）**のレイアウト構成、コンテンツの意味論的役割、およびエージェントの推論精度を最大化するためのドキュメント設計原則をまとめたデザインポリシーです。

AI（SpecWriter）が仕様書のテキストを推論・生成する際、およびテンプレートを合成する際は、本ドキュメントに記述された表現の規約を厳格に遵守してください。

---

## 1. 決定論と非決定論の完全分離（境界設計）

仕様書ドキュメントのデザインにおける最も本質的な思想は、**「決定論的（静的・ルールベース）に定義できる構造は100%プログラム/テンプレート側で出力し、非決定論的（動的・記述的）な文章の生成のみをLLMの表現に委ねる」** ことです。

* **決定論的構造 (プログラム/テンプレートの責務)**:
  * 見出し（`## 概要`、`### 主な機能`、`## トリガー条件`など）は、テンプレートファイル（`skill_spec.md.template`）側で静的に定義します。
  * 入力・出力パラメータテーブル、および制約事項の箇条書きは、`design.json` のデータをプログラムが直接パースし、決定論的にレンダリングします。LLMにテーブル自体を書かせることは禁止します。
* **非決定論的コンテンツ (LLM의 責務)**:
  * スキルが解決する本質的な目的、具体的な主要機能、実行時に起こる内部ステップといった、「人間や他のAIエージェントが意味を理解するための文章表現」の抽出・生成を担当します。

---

## 2. 概要（Overview）の意味論的構造化

LLMに巨大なテキストブロックを一括生成させると、JSONシリアライズの過程で改行が潰れる等のレイアウト崩れが発生します。
そのため、概要セクションは以下の3つの要素に明確に分解し、プログラムで安全にマークダウンへ結合します。

1. **目的・役割 (`purpose`)**:
   * **意味**: このスキルが解決する本質的な課題と価値を1〜2文で要約したもの。
   * **表現**: スキル概要の導入として最上部に配置します（※設計書に `summary` フィールドがある場合は、決定論的マスターデータとしてそれがそのまま採用されます）。
2. **主要機能 (`features`)**:
   * **意味**: スキルがサポートしている具体的な機能やカバー範囲のリスト。
   * **表現**: 箇条書き（`*`）のリストとして明示します。
3. **処理プロセス (`workflow_steps`)**:
   * **意味**: スキルが呼び出されたときの具体的な内部処理ステップ。
   * **表現**: 番号付きリスト（`1.`）として手順を論理的に記述します。

---

## 3. エージェント向けマニュアルの書き分け

仕様書における「AIエージェント向け使用方法」の手順書（Instructions）は、スキルの `execution_type`（実行タイプ）に応じて、AIが取るべき行動指針を明確に書き分けます。また、トークン数節約と注意力の最大化を図るため、冗長な共通指示を排除します。

* **`"tool"`（決定論的ツール）の手順書**:
  * **ポリシー**: 「AI自身は不要な推論を行わず、単に引数を決定してツールに処理を中継・委譲するだけでよい」という事実を強調し、パラメータの抽出に必要な最小限 of 指示のみを記述します。ボイラープレートのような長文のLLM共通ガイドライン（トリガー、引数マッピング、結果解釈など）は、トークン削減およびパラメータ決定の正確性向上の観点から完全に排除します。
* **`"agent"`（エージェント思考型）の手順書**:
  * **ポリシー**: 「AI自身がこのスキルの内部テンプレートやプロンプト指示をロードし、その手順に従って自律的な思考・テキスト生成を実行する必要がある」という手順を示します。

---

## 4. プロンプト引数（is_prompt_parameter）の境界定義と警告ガイドライン

設計ファイル（`design.json`）における `is_prompt_parameter` の有無は、AIエージェントの自律判断を介するかどうかによって厳格に区別して設計されなければなりません。

### ① パラメータの区別基準
* **自律的判断パラメータ (`is_prompt_parameter: true`)**:
  * スキル内部の決定論的なプログラム（Pythonコードなど）で直接処理されるものではなく、LLM（AI）による非決定論的な自律的判断や推論に直接引き渡されて利用される可変パラメータ（例: 自然言語の指示 `prompt` など）。
  * 呼び出し側のAIや人間が「どのような基準で判断され、どのような制約があるか」を事前に理解していないと、不適切なプロンプトが渡されて推論失敗や再試行ループが発生します。
* **通常のシステムパラメータ (`is_prompt_parameter: false`)**:
  * 決定論的なプログラム（`if` 文による数値比較、機械的なファイル読み込み、タイムアウト秒数など）によって直接解釈・処理されるシステム変数。
  * これらはLLMの自律判断を挟まないため、パラメータテーブルの機械的な制約説明のみで十分であり、個別ガイドラインによる強調は不要です。

### ② 仕様書（SKILL.md）における警告枠の役割
`is_prompt_parameter: true` なプロンプト型引数が定義されている場合、呼び出し側（親エージェント）や人間のユーザーに対して引数の仕様限界と契約（ハーネス）を強力に伝えるため、GitHub Alert の `[!NOTE]` 警告形式を用いて個別ガイドラインを生成します。

* **目的**: 呼び出し側が「ハーネスによる制約を破壊するような不正なプロンプト呼び出し」を何度も繰り返すことを防ぐための防護壁として機能させます。
* **表現フォーマット**:
  * 対象の関数の「入力パラメータテーブル」の直後に配置し、以下を明記します。
    * **指定可能な指示（`prompt_instructions`）**: プロンプトを通じて制御可能な指示内容（トーンの調整や背景コンテキストの強調など）。
    * **構造的な制約（`prompt_constraints`）**: ドキュメント構造やスキーマ定義自体など、プロンプト指示によっても変更不可能な領域の明示。

---

## 5. 複数関数の公開におけるカプセル化ポリシー

ADK 2.0 の思想において、1つのスキルパッケージが複数の公開関数（APIエンドポイント）を `scripts/handler.py` からエクスポートして提供する「マイクロサービス構造」をとる場合、`SKILL.md` も関数主軸の整理された構造で記述される必要があります。

### ① 情報の関数単位でのパッケージ化
* **ポリシー**: 複数の関数がある場合、入力パラメータ・出力パラメータ・実行指示といった情報をドキュメントの別々のセクション（例: 全体としての「パラメータ詳細」と「出力仕様」）に分散して配置することを禁止します。
* **表現構造**:
  - `## 公開関数 (APIエンドポイント)` という大セクションの下に、各関数個別の見出しを作成します。
  - 各関数の見出しの配下に、その関数固有の「関数の説明(description)」「実行方法」「入力パラメータ」「プロンプトガイドライン」「出力仕様」を完全にカプセル化して配置します。
  - これにより、他のLLMや人間が「ある特定の関数のみ」を呼び出す際の仕様の確認が1箇所で完結し、ドキュメントの上下の往復を避けることができます。

### ② 入力パラメータテーブルの強化
* **デフォルト値の明示**:
  - パラメータテーブルに「デフォルト値」カラムを追加し、オプション引数のデフォルト値を明記します。デフォルト値が定義されていない場合は `-` を表示します。

### ③ 出力仕様の記述規則
* **構造化出力 (`STRUCTURED_JSON`)**:
  - 出力モードが `STRUCTURED_JSON` であり、かつ関数に `response_parameters` が定義されている場合、出力パラメータの一覧を型および必須フラグとともにテーブル形式で記述します。
* **非構造化出力 (`VALUE_ONLY` / `CONVERSATIONAL`)**:
  - 各関数定義に紐づく戻り値の型（`response_type`。例: `str` や `list[str]` など）を `戻り値の型: [型名]` として明記し、戻り値の形式を明らかにします。
