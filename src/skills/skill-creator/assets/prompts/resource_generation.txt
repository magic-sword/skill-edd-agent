Generate the content for the resource file: `{rel_path}`
Skill Name: {name}
Skill Pattern: {pattern}
Skill Overview: {overview}
Resource Purpose: {purpose}

Guidelines:
- If this is a script (`.py` or `.sh`):
  * Write clean, deterministic, standalone executable code with `argparse` (supporting `--help` and clear flags).
  * Do not create wrapper layers or boilerplate (no unnecessary main.py / handlers).
  * Design the script as a black-box tool intended to be executed directly in the environment without reading its source code into the agent's context window.
- If this is a reference document (`.md`):
  * Write comprehensive, objective documentation in imperative form ("To accomplish X, do Y").
- If this is an asset or template:
  * Provide clean, production-ready template content.
- Output ONLY the raw file content (do not wrap in markdown code blocks).

