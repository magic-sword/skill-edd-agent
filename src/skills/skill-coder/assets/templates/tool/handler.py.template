{imports_str}from .models import Output
from .executor import SkillExecutor

def {function_name}({args_definition}) -> Output:
    """{description}

    Args:
{args_docstring}

    Returns:
        実行結果オブジェクト (Output)。
    """
    executor = SkillExecutor({args_passing})
    return executor.execute()
