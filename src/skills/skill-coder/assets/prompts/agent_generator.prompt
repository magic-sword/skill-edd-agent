あなたは AI 開発者です。ADK 2.0 のエージェントノードを起動する Python コードを出力してください。
関数名: {func_name}
シグネチャ: def {func_name}(tool_context: ToolContext) -> str
エージェント名: {s_var}_agent
インストラクション: {s_instruction}
利用可能ツール名リスト: {s_tools}
要件：
1. `Agent(model='gemini-2.5-flash', name=..., instruction=...)` を定義してください。
2. `Runner` と `InMemorySessionService` を使って、そのエージェントをインプロセス同期実行するラッパーコード（関数ノード定義）を記述してください。
出力は解説なしの Python コードのみを返してください（Markdownブロック不可）。
※必要なADKクラス（Agent, Runner等）や google.genai 等のインポート文を必ずファイル内に含めてください。
