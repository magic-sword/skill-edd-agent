---
name: developer-router
description: 要件プロンプトを分析し、単体スキル(skill)かワークフロー(workflow)かを分類してルーティングするスキル。
---

# developer-router

## 概要
ユーザーから提供された要件プロンプトを分析し、それを実現するために「新規の単体スキルを開発するべきか（`skill`）」、あるいは「既存のスキルを連携させたワークフローを構築すべきか（`workflow`）」を分類・判定するインテリジェントルーターです。

### 主な機能
*   既存スキル（インベントリ）一覧のロードとセマンティックな理解。
*   要件プロンプトから、既存スキルのみで構成可能かどうかの判定。
*   判定結果（`route`）およびその分析理由（`rationale`）の出力。
*   ワークフロー判定時の、推奨される依存スキル（`recommended_dependencies`）の自動抽出。

## トリガー条件
このツールは以下のようなインテントで起動されます：
- 「この機能要件 '...' は単体スキルとワークフローのどちらで開発すべきですか？」
- 「機能要件を分析して適切な開発ルートを判定してください。」

## 入力パラメータ
| パラメータ名 | 型 | 必須 | 説明 |
|---|---|---|---|
| prompt | str | はい | 開発したい機能の要件プロンプト。 |

## 出力パラメータ (構造化JSON)
| パラメータ名 | 型 | 必須 | 説明 |
|---|---|---|---|
| route | str | はい | 判定された開発ルート（'skill' または 'workflow'）。 |
| rationale | str | はい | そのルートに決定した分析理由。 |
| recommended_dependencies | list[str] | はい | ワークフローの場合に推奨される既存スキル名のリスト。単体スキルの場合は空リスト。 |
